# SaaS Stack Catalog

This directory follows the exact SaaS stack structure you requested.

- Each top-level folder is a capability area.
- Each subfolder is a provider or framework option.
- This tree is intended as a planning map and can be expanded with implementation notes per option.

Use this folder as the architecture map for planning, RFCs, and future changes.

Current active backend code:

- `SaaS-Stack/Backend/NodeJS/filmedme-api`
